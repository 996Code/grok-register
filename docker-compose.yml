name: grok-stack

services:
  # ──────────────────────────────────────────────
  # 代理：mihomo (Clash Meta)
  # 用户需在 mihomo/config.yaml 填入订阅链接
  # 仅容器内部网络可访问，不对外暴露端口
  # ──────────────────────────────────────────────
  mihomo:
    container_name: grok-mihomo
    image: docker.io/metacubex/mihomo:latest
    volumes:
      - ./mihomo/config.yaml:/root/.config/mihomo/config.yaml:ro
      - mihomo-data:/root/.config/mihomo
    environment:
      TZ: Asia/Shanghai
    restart: unless-stopped
    networks:
      - grok-net

  # ──────────────────────────────────────────────
  # API 网关：grok2api
  # 接收 OpenAI 兼容请求，转发到 grok.com
  # 对外暴露 8000 端口
  # ──────────────────────────────────────────────
  grok2api:
    container_name: grok2api
    image: ghcr.io/chenyme/grok2api:latest
    ports:
      - "8000:8000"
    volumes:
      - ./grok2api/config.yaml:/run/grok2api/config.yaml:ro
      - grok2api-data:/app/data
    environment:
      TZ: Asia/Shanghai
    restart: unless-stopped
    depends_on:
      - mihomo
    networks:
      - grok-net

  # ──────────────────────────────────────────────
  # 注册机：grok-register
  # 默认不启动，手动触发：
  #   docker compose run --rm grok-register
  # ──────────────────────────────────────────────
  grok-register:
    profiles: ["register"]
    container_name: grok-register
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./config.json:/app/config.json:ro
      - ./output:/app/output
    environment:
      TZ: Asia/Shanghai
    depends_on:
      - mihomo
      - grok2api
    networks:
      - grok-net

networks:
  grok-net:
    driver: bridge

volumes:
  mihomo-data:
  grok2api-data:
